"use strict";
let userName = ['admin', 'zeeshan', 'junaid', 'kamran', 'daniyal'];
if (userName.length === 0) {
    console.log('we need to find some users');
}
else {
    userName = [];
    console.log('all user name have been removed. ' + userName.length);
}
