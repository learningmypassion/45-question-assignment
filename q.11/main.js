//name:store names of a few of your friends in a array called names.
//print each persons name by accessing each element in the list,one at a time 
let friends_names = ['taha', 'nazia', 'ahmed'];
for (let i = 0; i < friends_names.length; i++) {
    console.log(friends_names[i]);
}
export {};
